name: Build (Linux, x86-64)

# TODO: test with only pull-request
on:
  push:
    paths: &workflow_paths
      - "**.cppm"
      - "**.ixx"
      - "**.inl"
      - "**.mpp"
      - "**.ipp"
      - "**.tpp"
      - "**.txx"
      - "**.inc"
      - "**.cpp"
      - "**.hpp"
      - "**.cxx"
      - "**.hxx"
      - "**.cc"
      - "**.hh"
      - "**.c"
      - "**.h"
      - ".github/workflows/build-linux.yml"
      - "**/meson.build"
      - "flake.nix"

  pull_request:
    paths: *workflow_paths

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !contains(github.ref, 'dev/') }}

jobs:
  build-linux-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Enter dev-shell
        uses: ./.github/workflows/composite/enter-devshell

      - name: Restore build cache
        uses: ./.github/workflows/composite/restore-cache
        with:
          ccache_dir: ${{ runner.temp }}/ccache

      - name: Build meson (release)
        uses: ./.github/workflows/build-meson
        with:
          dir_name: "build_release"
          setup_args: "--buildtype=release"

  build-linux-debug:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Enter dev-shell
        uses: ./.github/workflows/composite/enter-devshell

      - name: Restore build cache
        uses: ./.github/workflows/composite/restore-cache
        with:
          ccache_dir: ${{ runner.temp }}/ccache

      - name: Build meson (debug)
        uses: ./.github/workflows/build-meson
        with:
          dir_name: "build_debug"
          setup_args: "--buildtype=debug"
